html
  head
    meta(charset='utf-8')
    title #{title}
    h2(style="text-align: center") #{title}
  body
  each item in items
    div(style="text-align: center")
        a(href=item.link) #{item.title} 
        p #{item.contentSnippet}
        