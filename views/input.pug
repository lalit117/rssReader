html
  head
    meta(charset='utf-8')
    title #{title}
    style.
      div {
        width: 500px; 
        margin: 50px auto;
        padding: 16px;
      }
      form {
        border: 3px solid #f1f1f1;
      }
      input {
        		width: 80%;
            padding: 6px 5px;
            margin: 10px 0;
            border: 1px solid #ccc;
            box-sizing: border-box;
      }
      label {
        margin: 0 12px;
        display: inline-block;
      }
      button {
        width: 40%;
        margin: 5px auto 10px auto;
        display:block;
        padding: 3px 2px;
      }
  body
    div
      form(action='/', method='post')
          label Url :
          input(name='rssUrl', type='text', placeholder='Enter a Url', required='')
          br
          button(type='submit', ) Read